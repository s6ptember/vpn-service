# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Generated by drizzle-kit. Formatting these deadlocks CI against itself: prettier --write reflows
# meta/*.json, then the drift guard's `db:generate` rewrites them in drizzle-kit's own style, so
# `git status --porcelain drizzle/` is dirty on every run and reports a schema drift that does not
# exist. Leave them exactly as the generator writes them.
/drizzle/

# Contract documents. Lead-owned and append-only (tech.md 15) — prettier reflowing tables and
# wrapping prose would fight their authorship for no gain.
/tech.md
/CLAUDE.md

# Vendored design mock, not source.
/vpn-miniapp.html
